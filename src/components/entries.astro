---
import Content from "../layouts/Content.astro";
import { getEntry } from "astro:content";

const makisData = await getEntry('menu', 'entries');
---

<Content 
  category={makisData.data.category}
  items={makisData.data.items}
  title="ENTRADAS"
  subtitle=""
/>
